# Resi Frontend

PWA React que atende moradores, parceiros e prefeituras. Desenvolvido com Vite + TypeScript e identidade visual inspirada na Costa Esmeralda (verde esmeralda, areia clara).

## Scripts
```bash
cd frontend
npm install
npm run dev
```

O servidor de desenvolvimento proxya chamadas `/api` para o backend FastAPI (porta 8000).
